__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env

# The app's own data and caches, never the repo's.
cards/
transcripts/
recordings/
cappa/source/.cache/
lexicon_packs/
jmdict_packs/
arabic_packs/
settings.json

# Kept as a local-only working note, not tracked in the repo.
AGENTS.md
