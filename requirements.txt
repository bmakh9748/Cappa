PySide6
pywin32
mss
numpy
rapidocr
python-bidi  # rapidocr imports it on every RTL (arabic) read but doesn't declare it
onnxruntime-directml  # the GPU (DirectML) build — detection/gpu.py probes for
                      # it; plain `onnxruntime` also works but scans on CPU
                      # at the smaller working size (slower, misses small text).
                      # Upgrading an old venv: `pip uninstall onnxruntime`
                      # FIRST — both wheels own the same package directory.
anki
deep-translator
requests  # dictionary/pronounce/examples prefer it over urllib: this
          # machine's cert store rejects Wikimedia's TLS chain under bare
          # urllib (it rides in with deep-translator today, but the direct
          # import deserves a declaration — the python-bidi rule)
PyAudioWPatch
yt-dlp[default]
