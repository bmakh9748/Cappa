"""Japanese: the deepest of the three studied languages, and the only one
whose words have to be FOUND before they can be looked up (no spaces).

    jmdict.py    JMdict pack: word lookup + deinflection + word_at (where
                 does the word under the cursor end?), plus the grammar
                 notes explaining each deinflection reason. Offline.
    kanjidic.py  KANJIDIC2 pack: per-kanji meanings, readings, strokes,
                 grade and JLPT level for the Grammar tab. Offline.

Both lazy, fail-soft, no-pack-means-no-change. No Qt."""
